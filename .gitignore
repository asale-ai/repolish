/target
**/*.rs.bk
Cargo.lock.bak

# 内部判断（竞品调研 / 产品定位 / 路线图与决策记录）不进公开仓库。
# 留在工作副本里，不跟着 clone 走。
/.internal/

# ClawHub 令牌等本地凭据。publish-clawhub.sh 从这里读，绝不入库。
.env

# 现在没有任何东西会写这个文件——publish.sh 改成交互式发布之后，token 那套
# 全删了。留着这条是因为它挡的是**将来**：一个凭据文件被提交上去，撤销的代价
# 比这两行大得多。
npm/.npmrc

# `node npm/install.js` 在仓库里跑过的话会留下一个几兆的二进制。
# 它是下载物,不是源码。
npm/bin/repolish
npm/bin/repolish.exe
